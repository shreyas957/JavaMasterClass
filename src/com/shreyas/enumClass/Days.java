package com.shreyas.enumClass;

public enum Days {
    SUN, MON, TUE, WED, THURS, FRI, SAT
    // After the last constant semicolon is not required, but if we are further
    // add methods and constructors in enum class then semicolon is required.

}
